---
- name: Add Ansible service account to local Administrators
  hosts: all
  gather_facts: false

  vars_files:
    - vars/credentials.yaml

  tasks:
    - name: Ensure Ansible service account is in local Administrators
      ansible.windows.win_group_membership:
        name: Administrators
        members:
          - "{{ windows_automation_user }}"
        state: present